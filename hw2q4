def find_largest(numbers):
# puting numbers in a list    
    largest = numbers[0]
    for num in numbers:
# defining relationship      
      if num > largest:
            largest = num
    return largest

# taking input
numbers = [1, 2, 4, 5, 3]
largest = find_largest(numbers)
# returning output
print(largest)
