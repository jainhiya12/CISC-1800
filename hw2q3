def calculate_average(numbers):
    total = sum(numbers)
    amount = len(numbers)
# averaging numbers
    average = total / amount
    return average

# taking input here
numbers = [1, 2, 3, 4, 5]
average = calculate_average(numbers)
# printing output
print(average)
